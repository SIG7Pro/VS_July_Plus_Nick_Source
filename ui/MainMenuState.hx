function updatePost() {
//Value 1 is X, 2 is Y.
    state.menuItems.members[0].offset.set(380, 0);

    state.menuItems.members[1].offset.set(430, 10);

    state.menuItems.members[2].offset.set(410, 20);

    state.menuItems.members[3].offset.set(440, 30);

    state.menuItems.members[4].offset.set(250, 40);

    state.menuItems.members[5].offset.set(0, 50);

    state.menuItems.members[6].offset.set(-250, 60);

}